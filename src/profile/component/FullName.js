import react from 'react'
function FullName (props) {
    console.log(props)
    return
       (
        <div>

        <h2> {props.fullName} </h2>

        </div>
        )
}
 export default FullName
 